pipeline {
	agent any
	
	stages {
		stage('Build') {
			steps {
				echo 'Building v2'
			}
		}
		stage('Test') {
			steps {
				echo 'Testing v2'
			}
		}
		stage('Deploy') {
			steps {
				echo 'Deploying v2'
			}
		}
	}
}